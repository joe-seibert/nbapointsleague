[project]
name = "nbapointsleague"
version = "0.1.0"
description = "NBA fantasy basketball points league tracker"
requires-python = ">=3.8"
dependencies = [
    "pandas",
    "pyyaml",
    "nba-api",
    "google-auth-oauthlib",
    "google-auth",
    "google-api-python-client",
]
